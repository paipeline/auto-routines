# auto-routines: this repo IS the skill, so we ignore typical dev cruft only.
__pycache__/
*.pyc
.DS_Store
.venv/
venv/
node_modules/

# These files are generated by the skill IN A CONSUMING REPO,
# not in this skill repo. Listed here so contributors don't accidentally
# commit them if they test the skill in this directory:
/.iteration/
/.claude/settings.local.json
